---
"executor": patch
---

Allow explicitly selected Cloudflare Access service tokens to administer workspace settings through the `ADMIN_COMMON_NAMES` allowlist. Other service tokens remain workspace members without administrative write access.
