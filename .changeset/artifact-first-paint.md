---
"executor": patch
---

**Improve: newly generated artifacts keep their UI structure visible while live data loads.** Artifact guidance now places loading, error, and empty states inside their data regions instead of replacing the entire view.
